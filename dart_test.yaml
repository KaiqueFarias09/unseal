# Fuzz suite tag configuration.
#
# `fuzz` marks the long-running corpus campaigns. CI excludes the tag
# from its regular VM lane; invoke it explicitly with `dart test --tags fuzz`.
tags:
  fuzz:
    timeout: 30m
