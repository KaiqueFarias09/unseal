# Architecture

Generated by Steward from the resolved project standard.

- Contract family: `steward/publishable-package`
- Contract version: `1.0.0`
- Contract digest: `d3e879207e815bd952c41ba11b1d06b70ba3df08616864f104b983a819bfa00a`
- Resolution: `managed exact contract pin`

## Project

- Name: `e_livre`
- Kind: `dart-library`
- Description: Dart library onboarded for Steward migration.
- Managed: `true`

## Targets

- `example`: runtime `flutter`, artifact `app`, publishable `false`, managed `true`, capabilities `assets`, `example`, analysis options `flutter-example-v1`, formatter page width `100`, path `example`.
- `library`: runtime `dart`, artifact `library`, publishable `true`, managed `true`, capabilities `io`, analysis options `dart-library-v1`, formatter page width `100`, path `.`.

## Directory Roles

- `core`: Technical policies and types independent of product features. Patterns: `lib/core/**`, `lib/src/foundation/**`.
- `di`: Bootstrap, provider overrides, and the composition root. Patterns: `lib/di/**`.
- `feature/application`: Feature orchestration and use cases. Patterns: `lib/features/*/application/**`.
- `feature/contracts`: Cohesive public contracts owned by a feature. Patterns: `lib/features/*/contracts/**`, `lib/src/features/*/api/**`.
- `feature/data`: Feature data sources and persistence adapters. Patterns: `lib/features/*/data/**`, `lib/src/features/*/adapters/**`.
- `feature/domain`: Feature domain rules and cohesive value types. Patterns: `lib/features/*/domain/**`, `lib/src/features/*/domain/**`.
- `feature/presentation`: Feature state, screens, and UI components. Patterns: `lib/features/*/presentation/**`.
- `infrastructure`: Global database, network, filesystem, logging, and platform adapters. Patterns: `lib/infrastructure/**`.
- `presentation`: App shell, router, theme, and global navigation. Patterns: `lib/presentation/**`.
- `shared`: Product contracts or components with independent consumers. Patterns: `lib/shared/**`, `lib/src/shared/**`.
- `workflows`: Cross-feature coordination without a natural feature owner. Patterns: `lib/workflows/**`.

## Dependency Rules

- `core` -> `core`: allow.
- `di` -> `core`: allow.
- `di` -> `feature/application`: allow.
- `di` -> `feature/data`: allow.
- `di` -> `infrastructure`: allow.
- `di` -> `shared`: allow.
- `feature/application` -> `core`: allow.
- `feature/application` -> `feature/contracts`: allow.
- `feature/application` -> `feature/domain`: allow.
- `feature/application` -> `shared`: allow.
- `feature/contracts` -> `core`: allow.
- `feature/contracts` -> `feature/contracts`: allow.
- `feature/contracts` -> `shared`: allow.
- `feature/data` -> `core`: allow.
- `feature/data` -> `feature/contracts`: allow.
- `feature/data` -> `feature/domain`: allow.
- `feature/data` -> `infrastructure`: allow.
- `feature/data` -> `shared`: allow.
- `feature/domain` -> `core`: allow.
- `feature/domain` -> `feature/contracts`: allow.
- `feature/domain` -> `shared`: allow.
- `feature/presentation` -> `core`: allow.
- `feature/presentation` -> `feature/application`: allow.
- `feature/presentation` -> `feature/domain`: allow.
- `feature/presentation` -> `shared`: allow.
- `infrastructure` -> `core`: allow.
- `infrastructure` -> `shared`: allow.
- `presentation` -> `core`: allow.
- `presentation` -> `feature/contracts`: allow.
- `presentation` -> `shared`: allow.
- `shared` -> `core`: allow.
- `shared` -> `shared`: allow.
- `workflows` -> `core`: allow.
- `workflows` -> `feature/contracts`: allow.
- `workflows` -> `shared`: allow.

## Vocabulary

### Data types

- `boundary_projection`: `Dto` (examples: `MangaDto`)
- `call_specific_options`: `OperationOptions` (examples: `SearchOptions`)
- `controller_state`: `FeatureState` (examples: `ReaderState`)
- `database_record`: `Row` (examples: `MangaRow`)
- `domain_value`: `noun` (examples: `Account`, `Deck`, `Manga`)
- `durable_configuration`: `CapabilityConfig` (examples: `ReaderConfig`)
- `operation_input`: `OperationParams` (examples: `SearchParams`)
- `screen_projection`: `ScreenViewData` (examples: `HomeViewData`)
- `transport_input`: `ActionRequest`
- `transport_output`: `ConceptResponse`

### Working classes

- `bidirectional_encoding`: `Codec`
- `capability_detection`: `Detector`
- `cohesive_capability`: `Service`
- `compression`: `Compressor`
- `conversion`: `Converter`
- `decoding`: `Decoder`
- `decompression`: `Decompressor`
- `domain_access`: `Repository`
- `domain_sniffing`: `Sniffer`
- `encoding`: `Encoder`
- `external_protocol`: `Client`
- `implementation_selection`: `Resolver`
- `input_reading`: `Reader`
- `interface_conversion`: `Adapter`
- `library_assembly`: `Assembler`
- `local_persistence`: `Store`
- `multi_collaborator_workflow`: `Coordinator`
- `platform_bridge`: `Bridge`
- `presentation_state`: `Controller`
- `processing_engine`: `Engine`
- `protocol_server`: `Server`
- `pure_business_decision`: `Policy`
- `rendering`: `Renderer`
- `side_effect_free_validation`: `Validator`
- `stable_subsystem_api`: `Facade`
- `structural_conversion`: `Mapper`
- `structured_interpretation`: `Parser`

### Parameters

- Shared: `At timestamps`, `Duration`, `full dependency names`, `is/has/can/should booleans`
- Dart: `chapterId`, `mangaId`, `userId`
- Go: `chapterID`, `mangaID`, `userID`

### Retired terms

- `ApiService`, `ControllerState`, `Helper`, `Manager`, `Model`, `PersistenceHelper`, `TagManagerService`, `Utils`

## Declaration Policy

- One primary declaration per file: `true`.
- Type-owned modules: supporting declarations above the primary type: `true`; primary type last: `true`.
- Function-owned modules: one public primary function first: `true`; reachable private helpers below it: `true`.
- Supporting classes: at most `1`, with a maximum of `40` lines; peer severity: `gate`.
- Generated declarations exempt: `true`; sealed families allowed: `true`.

## Active Rules

- `automatic_keep_alive_missing_super` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.automatic_keep_alive_missing_super).
- `controller_not_disposed` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.controller_not_disposed).
- `dart.declaration.ownership` on `example`: `gate` — rule has no selectors and applies to every target (provenance: architecture contract, applicability.dart.declaration.ownership).
- `dart.naming.vocabulary` on `example`: `gate` — rule has no selectors and applies to every target (provenance: architecture contract, applicability.dart.naming.vocabulary).
- `dart.private-named-parameters` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart.private-named-parameters).
- `dart_build_formatting.build_blank_line_after_preamble` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_build_formatting.build_blank_line_after_preamble).
- `dart_build_formatting.build_blank_line_between_builder_phases` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_build_formatting.build_blank_line_between_builder_phases).
- `dart_build_formatting.build_no_blank_lines_in_children` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_build_formatting.build_no_blank_lines_in_children).
- `dart_build_formatting.build_no_blank_lines_in_constructor_args` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_build_formatting.build_no_blank_lines_in_constructor_args).
- `dart_build_formatting.build_no_blank_lines_in_stack_children` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_build_formatting.build_no_blank_lines_in_stack_children).
- `dart_member_ordering.class_build_method_first` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_build_method_first).
- `dart_member_ordering.class_constructor_first` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_constructor_first).
- `dart_member_ordering.class_consumer_widget_stores_ref_or_context` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_consumer_widget_stores_ref_or_context).
- `dart_member_ordering.class_field_group_order` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_field_group_order).
- `dart_member_ordering.class_field_groups_compact_alphabetical` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_field_groups_compact_alphabetical).
- `dart_member_ordering.class_getters_grouped_alphabetical` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_getters_grouped_alphabetical).
- `dart_member_ordering.class_member_group_order` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_member_group_order).
- `dart_member_ordering.class_private_methods_after_public` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_private_methods_after_public).
- `dart_member_ordering.class_private_methods_alphabetical` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_private_methods_alphabetical).
- `dart_member_ordering.class_private_widget_helper_method` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_private_widget_helper_method).
- `dart_member_ordering.class_public_methods_alphabetical` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_public_methods_alphabetical).
- `dart_member_ordering.class_state_lifecycle_first` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_state_lifecycle_first).
- `dart_member_ordering.class_static_before_instance` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_static_before_instance).
- `dart_member_ordering.class_static_methods_last_alphabetical` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.class_static_methods_last_alphabetical).
- `dart_member_ordering.file_supporting_types_last` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.file_supporting_types_last).
- `dart_member_ordering.model_constructor_parameter_order` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.model_constructor_parameter_order).
- `dart_member_ordering.model_field_order` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.model_field_order).
- `dart_member_ordering.model_member_group_order` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_member_ordering.model_member_group_order).
- `dart_quality.boolean-parameter` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.boolean-parameter).
- `dart_quality.class-field-method-balance` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.class-field-method-balance).
- `dart_quality.class-method-count` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.class-method-count).
- `dart_quality.class-size` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.class-size).
- `dart_quality.complexity-bypass-extension` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.complexity-bypass-extension).
- `dart_quality.cyclomatic-complexity` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.cyclomatic-complexity).
- `dart_quality.efferent-coupling` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.efferent-coupling).
- `dart_quality.entangled-class` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.entangled-class).
- `dart_quality.extension-getter-placement` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.extension-getter-placement).
- `dart_quality.function-size` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.function-size).
- `dart_quality.guard-clause-spacing` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.guard-clause-spacing).
- `dart_quality.inheritance-depth` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.inheritance-depth).
- `dart_quality.lcom4` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.lcom4).
- `dart_quality.lcom4` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.lcom4).
- `dart_quality.local-variable-proximity` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.local-variable-proximity).
- `dart_quality.mixin-count` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.mixin-count).
- `dart_quality.nested-guard-clause-candidate` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.nested-guard-clause-candidate).
- `dart_quality.nesting-depth` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.nesting-depth).
- `dart_quality.no-blank-line-before-single-return` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.no-blank-line-before-single-return).
- `dart_quality.no-blank-line-before-single-return` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.no-blank-line-before-single-return).
- `dart_quality.no-nested-ternary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.no-nested-ternary).
- `dart_quality.no-silent-catch` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.no-silent-catch).
- `dart_quality.no-vague-public-names` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.no-vague-public-names).
- `dart_quality.number-of-children` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.number-of-children).
- `dart_quality.parameter-count` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.parameter-count).
- `dart_quality.prefer-conditional-return` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.prefer-conditional-return).
- `dart_quality.private-helper-order` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.private-helper-order).
- `dart_quality.response-for-class` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.response-for-class).
- `dart_quality.return-spacing` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.return-spacing).
- `dart_quality.semantic-architecture-drift` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-architecture-drift).
- `dart_quality.semantic-duplicate-method` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-duplicate-method).
- `dart_quality.semantic-duplicate-responsibility` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-duplicate-responsibility).
- `dart_quality.semantic-duplicate-widget` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-duplicate-widget).
- `dart_quality.semantic-god-class` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-god-class).
- `dart_quality.semantic-lcom4` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-lcom4).
- `dart_quality.semantic-lcom4` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-lcom4).
- `dart_quality.semantic-misplaced-method` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-misplaced-method).
- `dart_quality.semantic-naming-mismatch` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-naming-mismatch).
- `dart_quality.semantic-thin-wrapper` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.semantic-thin-wrapper).
- `dart_quality.split-candidate` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.split-candidate).
- `dart_quality.todo-policy` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.todo-policy).
- `dart_quality.weighted-methods-per-class` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_quality.weighted-methods-per-class).
- `dart_statement_formatting.compact_guard_clause` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_statement_formatting.compact_guard_clause).
- `dart_statement_formatting.max_line_width` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_statement_formatting.max_line_width).
- `dart_statement_formatting.page_width` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.dart_statement_formatting.page_width).
- `expensive_operation_in_build` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.expensive_operation_in_build).
- `flutter.a11y-copy-through-l10n` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.a11y-copy-through-l10n).
- `flutter.a11y-custom-tap-target` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.a11y-custom-tap-target).
- `flutter.a11y-icon-button-label` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.a11y-icon-button-label).
- `flutter.a11y-image-semantics` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.a11y-image-semantics).
- `flutter.a11y-no-text-scale-disable` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.a11y-no-text-scale-disable).
- `flutter.analysis-options-standard` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.analysis-options-standard).
- `flutter.analysis-options.out-of-sync` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.analysis-options.out-of-sync).
- `flutter.analytics-event-contract` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.analytics-event-contract).
- `flutter.animated-builder-child` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.animated-builder-child).
- `flutter.animated-clipping-performance` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.animated-clipping-performance).
- `flutter.app-interaction-surfaces` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.app-interaction-surfaces).
- `flutter.app.data-vocabulary` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.app.data-vocabulary).
- `flutter.app.dependency-matrix` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.app.dependency-matrix).
- `flutter.app.feature-contract-boundary` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.app.feature-contract-boundary).
- `flutter.app.feature-first` on `example`: `gate` — selector matches runtime=flutter artifact=app (provenance: architecture contract, applicability.flutter.app.feature-first).
- `flutter.app.feature-ownership` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.app.feature-ownership).
- `flutter.app.global-application-directory` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.app.global-application-directory).
- `flutter.app.no-global-application` on `example`: `gate` — selector matches runtime=flutter artifact=app (provenance: architecture contract, applicability.flutter.app.no-global-application).
- `flutter.app.parameter-vocabulary` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.app.parameter-vocabulary).
- `flutter.app.working-class-boundary` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.app.working-class-boundary).
- `flutter.application-cross-feature-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.application-cross-feature-location).
- `flutter.asset-registry-usage` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.asset-registry-usage).
- `flutter.backend-api-transport-bypass` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.backend-api-transport-bypass).
- `flutter.barrel-exports` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.barrel-exports).
- `flutter.barrel-exports` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.barrel-exports).
- `flutter.bug-fix-regression-tests` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.bug-fix-regression-tests).
- `flutter.build-logic-in-build` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.build-logic-in-build).
- `flutter.callbacks-in-views` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.callbacks-in-views).
- `flutter.canvas-save-layer-performance` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.canvas-save-layer-performance).
- `flutter.clip-save-layer-performance` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.clip-save-layer-performance).
- `flutter.constants-structure` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.constants-structure).
- `flutter.constants-structure` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.constants-structure).
- `flutter.constants-structure` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.constants-structure).
- `flutter.constants-structure` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.constants-structure).
- `flutter.constants-structure` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.constants-structure).
- `flutter.context-cache-alias` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.context-cache-alias).
- `flutter.context-dependency-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.context-dependency-boundary).
- `flutter.context-read-notifier` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.context-read-notifier).
- `flutter.controller-dependency-lookup` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.controller-dependency-lookup).
- `flutter.controller-part-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.controller-part-location).
- `flutter.controller-root-layout` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.controller-root-layout).
- `flutter.controller-state-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.controller-state-location).
- `flutter.controller-support-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.controller-support-location).
- `flutter.controller-test-required` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.controller-test-required).
- `flutter.cross-feature-import` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.cross-feature-import).
- `flutter.declaration-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.declaration-location).
- `flutter.dependency-validator-config` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.dependency-validator-config).
- `flutter.dependency-validator-gate` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.dependency-validator-gate).
- `flutter.derived-value-in-build` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.derived-value-in-build).
- `flutter.design-system-component-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.design-system-component-boundary).
- `flutter.design-token-semantics` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.design-token-semantics).
- `flutter.dio-decoded-api` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.dio-decoded-api).
- `flutter.dio-http-client` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.dio-http-client).
- `flutter.dio-native-architecture` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.dio-native-architecture).
- `flutter.dio-v2-api-contract` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.dio-v2-api-contract).
- `flutter.drift-untyped-sql-adapter` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.drift-untyped-sql-adapter).
- `flutter.duplicate-method-body` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.duplicate-method-body).
- `flutter.duration-token-conventions` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.duration-token-conventions).
- `flutter.e2e-cache-awareness` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.e2e-cache-awareness).
- `flutter.e2e-key-contract` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.e2e-key-contract).
- `flutter.e2e-key-contract` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.e2e-key-contract).
- `flutter.e2e-key-contract` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.e2e-key-contract).
- `flutter.e2e-scenario-shape` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.e2e-scenario-shape).
- `flutter.e2e-structure` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.e2e-structure).
- `flutter.e2e-structure` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.e2e-structure).
- `flutter.empty-architecture-folder` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.empty-architecture-folder).
- `flutter.entry-point-layout` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.entry-point-layout).
- `flutter.enum-companion-layout` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.enum-companion-layout).
- `flutter.enum-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.enum-location).
- `flutter.env-access-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.env-access-boundary).
- `flutter.env-config-public-contract` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.env-config-public-contract).
- `flutter.envied-standard` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.envied-standard).
- `flutter.error-code-sync` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.error-code-sync).
- `flutter.error-code-sync` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.error-code-sync).
- `flutter.error-response-body-preservation` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.error-response-body-preservation).
- `flutter.example.public-imports` on `example`: `gate` — selector matches runtime=flutter artifact=app (provenance: architecture contract, applicability.flutter.example.public-imports).
- `flutter.explicit-model-constructors` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.explicit-model-constructors).
- `flutter.extension-folder-api` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.extension-folder-api).
- `flutter.extract-local-data-constant` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.extract-local-data-constant).
- `flutter.failure-algebra-layout` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.failure-algebra-layout).
- `flutter.failure-code-localization-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.failure-code-localization-boundary).
- `flutter.failure-copy-through-l10n` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.failure-copy-through-l10n).
- `flutter.failure-display-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.failure-display-boundary).
- `flutter.failure-localization-standard` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.failure-localization-standard).
- `flutter.failure-message-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.failure-message-boundary).
- `flutter.failure-state-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.failure-state-boundary).
- `flutter.feature-first-architecture` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.feature-first-architecture).
- `flutter.feature-import-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.feature-import-boundary).
- `flutter.flavor-config-standard` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.flavor-config-standard).
- `flutter.fragmented-model-parsers` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.fragmented-model-parsers).
- `flutter.generated-model-copy-with` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.generated-model-copy-with).
- `flutter.generated-model-from-json` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.generated-model-from-json).
- `flutter.generated-model-to-json` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.generated-model-to-json).
- `flutter.immutable-models` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.immutable-models).
- `flutter.infrastructure-import-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.infrastructure-import-boundary).
- `flutter.intrinsic-layout-performance` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.intrinsic-layout-performance).
- `flutter.l10n-format` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.l10n-format).
- `flutter.l10n-identical-translation` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.l10n-identical-translation).
- `flutter.l10n-locale-coverage` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.l10n-locale-coverage).
- `flutter.l10n-metadata-required` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.l10n-metadata-required).
- `flutter.l10n-prune-unused` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.l10n-prune-unused).
- `flutter.layer-architecture` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.layer-architecture).
- `flutter.layer-constant-boundaries` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.layer-constant-boundaries).
- `flutter.lib-root-layout` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.lib-root-layout).
- `flutter.local-widgets-private` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.local-widgets-private).
- `flutter.material-motion-patterns` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.material-motion-patterns).
- `flutter.meaningful-widget-names` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.meaningful-widget-names).
- `flutter.mock-data-location` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.mock-data-location).
- `flutter.mock-data-usage` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.mock-data-usage).
- `flutter.model-codegen` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.model-codegen).
- `flutter.model-location-boundary` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.model-location-boundary).
- `flutter.motion-semantics` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.motion-semantics).
- `flutter.navigation-control-boundary` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.navigation-control-boundary).
- `flutter.navigation-framework-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.navigation-framework-boundary).
- `flutter.network-boundary` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.network-boundary).
- `flutter.no-accidental-exports` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-accidental-exports).
- `flutter.no-accidental-exports` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-accidental-exports).
- `flutter.no-build-side-effects` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-build-side-effects).
- `flutter.no-context-watch-outside-build` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-context-watch-outside-build).
- `flutter.no-context-watch-outside-build` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-context-watch-outside-build).
- `flutter.no-dependency-resolution-in-build` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-dependency-resolution-in-build).
- `flutter.no-direct-supabase` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-direct-supabase).
- `flutter.no-dual-di-container` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-dual-di-container).
- `flutter.no-duplicate-scalar-literals` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-duplicate-scalar-literals).
- `flutter.no-duplicate-string-literals` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-duplicate-string-literals).
- `flutter.no-feature-state-in-components` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-feature-state-in-components).
- `flutter.no-getit` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-getit).
- `flutter.no-getit-package` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-getit-package).
- `flutter.no-global-dao-layer` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-global-dao-layer).
- `flutter.no-hardcoded-user-copy` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-hardcoded-user-copy).
- `flutter.no-hollow-route-views` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-hollow-route-views).
- `flutter.no-inline-toggle-branching` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-inline-toggle-branching).
- `flutter.no-legacy-provider-naming` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-legacy-provider-naming).
- `flutter.no-local-widget-variables-in-build` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-local-widget-variables-in-build).
- `flutter.no-navigation-in-components` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-navigation-in-components).
- `flutter.no-notifier-parameters` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-notifier-parameters).
- `flutter.no-presentation-state-comparisons` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-presentation-state-comparisons).
- `flutter.no-production-e2e-branches` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-production-e2e-branches).
- `flutter.no-provider-in-components` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-provider-in-components).
- `flutter.no-raw-feature-buttons` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-buttons).
- `flutter.no-raw-feature-chips-badges` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-chips-badges).
- `flutter.no-raw-feature-dialogs` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-dialogs).
- `flutter.no-raw-feature-inputs` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-inputs).
- `flutter.no-raw-feature-list-items` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-list-items).
- `flutter.no-raw-feature-media` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-media).
- `flutter.no-raw-feature-menu-inputs` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-menu-inputs).
- `flutter.no-raw-feature-picker-inputs` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-picker-inputs).
- `flutter.no-raw-feature-scrollables` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-scrollables).
- `flutter.no-raw-feature-selection-inputs` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-selection-inputs).
- `flutter.no-raw-feature-surface-recipes` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-surface-recipes).
- `flutter.no-raw-feature-surfaces` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-surfaces).
- `flutter.no-raw-feature-text-inputs` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-text-inputs).
- `flutter.no-raw-feature-typography` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-typography).
- `flutter.no-raw-feature-value-inputs` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-feature-value-inputs).
- `flutter.no-raw-tap-targets` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-raw-tap-targets).
- `flutter.no-repository-service-in-views` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-repository-service-in-views).
- `flutter.no-router-provider` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-router-provider).
- `flutter.no-shared-raw-text-inputs` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-shared-raw-text-inputs).
- `flutter.no-single-use-shell-widgets` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-single-use-shell-widgets).
- `flutter.no-single-use-trivial-render-helpers` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-single-use-trivial-render-helpers).
- `flutter.no-thin-callback-wrappers` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-thin-callback-wrappers).
- `flutter.no-thin-design-system-widget-wrappers` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-thin-design-system-widget-wrappers).
- `flutter.no-ui-helper-methods` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-ui-helper-methods).
- `flutter.no-view-business-branching` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-view-business-branching).
- `flutter.no-view-business-logic` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-view-business-logic).
- `flutter.no-view-data-catalog` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-view-data-catalog).
- `flutter.no-view-state-derivation` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-view-state-derivation).
- `flutter.no-view-state-derivation` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-view-state-derivation).
- `flutter.no-view-state-derivation` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-view-state-derivation).
- `flutter.no-view-state-derivation` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-view-state-derivation).
- `flutter.no-view-state-derivation` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-view-state-derivation).
- `flutter.no-view-state-derivation` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-view-state-derivation).
- `flutter.no-widget-equality-override` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.no-widget-equality-override).
- `flutter.non-presentation-user-copy-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.non-presentation-user-copy-boundary).
- `flutter.opacity-animation-performance` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.opacity-animation-performance).
- `flutter.over-fragmented-data-helper` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.over-fragmented-data-helper).
- `flutter.oversized-image-decode` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.oversized-image-decode).
- `flutter.part-library-folder` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.part-library-folder).
- `flutter.patrol-e2e-standard` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.patrol-e2e-standard).
- `flutter.patrol-native-config` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.patrol-native-config).
- `flutter.permission-boundary` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.permission-boundary).
- `flutter.persistence-helper-no-thin-wrapper` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.persistence-helper-no-thin-wrapper).
- `flutter.precise-build-locals` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.precise-build-locals).
- `flutter.precise-build-locals` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.precise-build-locals).
- `flutter.presentation-cache-management` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.presentation-cache-management).
- `flutter.presentation-no-future-stream-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.presentation-no-future-stream-boundary).
- `flutter.presentation-no-setstate-in-views` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.presentation-no-setstate-in-views).
- `flutter.presentation-no-try-catch-in-ui` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.presentation-no-try-catch-in-ui).
- `flutter.primary-navigation-responsive-shell` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.primary-navigation-responsive-shell).
- `flutter.primary-navigation-shell` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.primary-navigation-shell).
- `flutter.primary-navigation-stack` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.primary-navigation-stack).
- `flutter.production-error-reporting` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.production-error-reporting).
- `flutter.provider-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.provider-location).
- `flutter.public-part-api` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.public-part-api).
- `flutter.repository-base-infrastructure` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.repository-base-infrastructure).
- `flutter.repository-duplicated-cache` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.repository-duplicated-cache).
- `flutter.repository-execute-operation` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.repository-execute-operation).
- `flutter.repository-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.repository-location).
- `flutter.repository-persistence-helper-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.repository-persistence-helper-boundary).
- `flutter.repository-test-required` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.repository-test-required).
- `flutter.repository-user-copy-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.repository-user-copy-boundary).
- `flutter.result-alias-layout` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.result-alias-layout).
- `flutter.result-future-repository` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.result-future-repository).
- `flutter.result-type-alias` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.result-type-alias).
- `flutter.review-boundary` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.review-boundary).
- `flutter.riverpod-broad-route-watch` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-broad-route-watch).
- `flutter.riverpod-controller-naming` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-controller-naming).
- `flutter.riverpod-controller-revision-state` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-controller-revision-state).
- `flutter.riverpod-controller-shape` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-controller-shape).
- `flutter.riverpod-controller-shape` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-controller-shape).
- `flutter.riverpod-controller-shape` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-controller-shape).
- `flutter.riverpod-controller-update-mixins` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-controller-update-mixins).
- `flutter.riverpod-duplicate-watch-scope` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-duplicate-watch-scope).
- `flutter.riverpod-freezed-state` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-freezed-state).
- `flutter.riverpod-immutable-state` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-immutable-state).
- `flutter.riverpod-lifecycle-provider-mutation` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-lifecycle-provider-mutation).
- `flutter.riverpod-listener-deferred-cleanup` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-listener-deferred-cleanup).
- `flutter.riverpod-no-change-notifier-state` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-change-notifier-state).
- `flutter.riverpod-no-change-tick-provider` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-change-tick-provider).
- `flutter.riverpod-no-compat-typedefs` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-compat-typedefs).
- `flutter.riverpod-no-compatibility-glue` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-compatibility-glue).
- `flutter.riverpod-no-controller-listeners` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-controller-listeners).
- `flutter.riverpod-no-controller-view-provider` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-controller-view-provider).
- `flutter.riverpod-no-generic-status-enum` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-generic-status-enum).
- `flutter.riverpod-no-listener-bridge` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-listener-bridge).
- `flutter.riverpod-no-notify-listeners` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-notify-listeners).
- `flutter.riverpod-no-private-controller-state` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-private-controller-state).
- `flutter.riverpod-no-provider-package` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-provider-package).
- `flutter.riverpod-no-public-mutable-controller-fields` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-public-mutable-controller-fields).
- `flutter.riverpod-no-ref-in-components` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-ref-in-components).
- `flutter.riverpod-no-state-notifier` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-state-notifier).
- `flutter.riverpod-no-view-controller-cache` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-view-controller-cache).
- `flutter.riverpod-no-watch-in-components` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-no-watch-in-components).
- `flutter.riverpod-notifier-wrapper-initialization` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-notifier-wrapper-initialization).
- `flutter.riverpod-prefer-async-value` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-prefer-async-value).
- `flutter.riverpod-provider-factory-exemption` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-provider-factory-exemption).
- `flutter.riverpod-provider-required` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-provider-required).
- `flutter.riverpod-provider-scope` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-provider-scope).
- `flutter.riverpod-select-single-member` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-select-single-member).
- `flutter.riverpod-single-state-assignment` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-single-state-assignment).
- `flutter.riverpod-state-watch` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-state-watch).
- `flutter.riverpod-unused-watch-in-build` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-unused-watch-in-build).
- `flutter.riverpod-view-state-access` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.riverpod-view-state-access).
- `flutter.route-extra-contract-purity` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.route-extra-contract-purity).
- `flutter.route-view-name-reserved` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.route-view-name-reserved).
- `flutter.routes-contract-location` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.routes-contract-location).
- `flutter.routes-import-boundary` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.routes-import-boundary).
- `flutter.routes-module-location` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.routes-module-location).
- `flutter.routes-root-layout` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.routes-root-layout).
- `flutter.routes-router-purity` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.routes-router-purity).
- `flutter.routes-single-source` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.routes-single-source).
- `flutter.runtime.container` on `example`: `gate` — selector matches runtime=flutter artifact=app (provenance: architecture contract, applicability.flutter.runtime.container).
- `flutter.save-layer-performance` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.save-layer-performance).
- `flutter.sealed-failure-hierarchy` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.sealed-failure-hierarchy).
- `flutter.semantic-colors` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.semantic-colors).
- `flutter.semantic-colors` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.semantic-colors).
- `flutter.semantic-function-names` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.semantic-function-names).
- `flutter.semantic-variable-names` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.semantic-variable-names).
- `flutter.service-location-boundary` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.service-location-boundary).
- `flutter.shared-import-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.shared-import-boundary).
- `flutter.shared-widget-api-usage` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.shared-widget-api-usage).
- `flutter.shared-widget-golden-required` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.shared-widget-golden-required).
- `flutter.single-use-function-file` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.single-use-function-file).
- `flutter.single-use-top-level-constant` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.single-use-top-level-constant).
- `flutter.standard-config-hygiene` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.standard-config-hygiene).
- `flutter.standard-packages` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.standard-packages).
- `flutter.state-update-mixins` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.state-update-mixins).
- `flutter.string-buffer-in-loops` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.string-buffer-in-loops).
- `flutter.telemetry-boundary` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.telemetry-boundary).
- `flutter.test-assertion-quality` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.test-assertion-quality).
- `flutter.test-helper-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.test-helper-location).
- `flutter.test-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.test-location).
- `flutter.test-package-standard` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.test-package-standard).
- `flutter.theme-extension-accessor-naming` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.theme-extension-accessor-naming).
- `flutter.theme-foundation-layout` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.theme-foundation-layout).
- `flutter.thin-facade-proxy` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.thin-facade-proxy).
- `flutter.thin-repository-proxy` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.thin-repository-proxy).
- `flutter.top-level-declaration-composition` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.top-level-declaration-composition).
- `flutter.typed-asset-references` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.typed-asset-references).
- `flutter.typed-go-router` on `example`: `warn` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.typed-go-router).
- `flutter.unread-di-singleton` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.unread-di-singleton).
- `flutter.use-project-icons` on `example`: `gate` — rule target "flutter" artifacts [app] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.use-project-icons).
- `flutter.use-project-logger` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.use-project-logger).
- `flutter.use-project-logger` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.use-project-logger).
- `flutter.use-project-logger` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.use-project-logger).
- `flutter.use-project-logger` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.use-project-logger).
- `flutter.use-project-logger` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.use-project-logger).
- `flutter.view-data-location` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.view-data-location).
- `flutter.view-file-focused-composition` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.view-file-focused-composition).
- `flutter.view-folder-signal` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.view-folder-signal).
- `flutter.view-module-folder` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.view-module-folder).
- `flutter.view-notifier-ownership` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.view-notifier-ownership).
- `flutter.view-notifier-ownership` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.view-notifier-ownership).
- `flutter.view-part-policy` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.view-part-policy).
- `flutter.widget-constructor-parameter-order` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.widget-constructor-parameter-order).
- `flutter.widget-field-order` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.widget-field-order).
- `flutter.widget-file-composition` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.widget-file-composition).
- `flutter.widget-file-composition` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.widget-file-composition).
- `flutter.widget-file-composition` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.widget-file-composition).
- `flutter.widget-file-composition` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.widget-file-composition).
- `flutter.widget-file-name` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter.widget-file-name).
- `flutter_context_cache_lookup` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.flutter_context_cache_lookup).
- `global_key_in_build` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.global_key_in_build).
- `inherited_widget_in_init_state` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.inherited_widget_in_init_state).
- `list_view_eager_children` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.list_view_eager_children).
- `media_query_full_subscription` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.media_query_full_subscription).
- `missing_repaint_boundary` on `example`: `info` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.missing_repaint_boundary).
- `opacity_constant_value` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.opacity_constant_value).
- `shrinkwrap_lazy_list` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.shrinkwrap_lazy_list).
- `stream_subscription_not_cancelled` on `example`: `gate` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.stream_subscription_not_cancelled).
- `unstable_key_in_build` on `example`: `warn` — rule target "flutter" artifacts [] matches target "example" runtime "flutter" artifact "app" (provenance: standard baseline, rules.unstable_key_in_build).
- `dart.analysis-options.out-of-sync` on `library`: `gate` — selector matches runtime=dart artifact=library (provenance: architecture contract, applicability.dart.analysis-options.out-of-sync).
- `dart.declaration.ownership` on `library`: `gate` — rule has no selectors and applies to every target (provenance: architecture contract, applicability.dart.declaration.ownership).
- `dart.library.public-entry-points` on `library`: `gate` — selector matches runtime=dart artifact=library (provenance: architecture contract, applicability.dart.library.public-entry-points).
- `dart.naming.vocabulary` on `library`: `gate` — rule has no selectors and applies to every target (provenance: architecture contract, applicability.dart.naming.vocabulary).
- `dart.package.publishability` on `library`: `gate` — selector matches runtime=dart artifact=library (provenance: architecture contract, applicability.dart.package.publishability).

## Exceptions

- None declared.

## Documentation Outputs

- `AGENTS.md`: `managed_block` — Managed architecture guidance.
- `CLAUDE.md`: `managed_block` — Managed architecture guidance.
- `GEMINI.md`: `managed_block` — Managed architecture guidance.
- `docs/architecture.md`: `complete` — Complete generated architecture contract projection.

## Commands


## Configuration

- No project-specific exclusions, overrides, or policies.

## Scaffold And Migration Recipes

- None declared.

## Provenance

- `architecture.digest`: architecture contract (`digest`) — stable digest of the resolved contract.
- `architecture.family`: architecture contract (`identity.family`) — resolved contract identity.
- `architecture.version`: architecture contract (`identity.version`) — resolved contract identity.
- `target.example.artifact`: steward.yaml (`example.artifact`) — resolved from flutter target metadata.
- `target.example.capabilities.assets`: steward.yaml (`example.capability assets`) — resolved from flutter target metadata.
- `target.example.capabilities.example`: steward.yaml (`example.capability example`) — resolved from flutter target metadata.
- `target.example.path`: steward.yaml (`example.path`) — resolved from flutter target metadata.
- `target.example.publishable`: steward.yaml (`example.publishable`) — resolved from flutter target metadata.
- `target.example.runtime`: steward.yaml (`example.runtime`) — resolved from flutter target metadata.
- `target.library.artifact`: steward.yaml (`..artifact`) — resolved from dart target metadata.
- `target.library.capabilities.io`: steward.yaml (`..capability io`) — resolved from dart target metadata.
- `target.library.path`: steward.yaml (`..path`) — resolved from dart target metadata.
- `target.library.publishable`: steward.yaml (`..publishable`) — resolved from dart target metadata.
- `target.library.runtime`: steward.yaml (`..runtime`) — resolved from dart target metadata.

