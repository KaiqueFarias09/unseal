name: unseal_example
description: Example app for the unseal Dart library.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  unseal:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/Alices Adventures in Wonderland.epub
