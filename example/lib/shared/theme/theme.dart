import 'package:flutter/widgets.dart';

/// Spacing tokens used by the example application.
abstract final class AppSpacing {
  /// Medium vertical gap between summary rows.
  static const Widget vMd = SizedBox(height: 16);
}
